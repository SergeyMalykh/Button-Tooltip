import { CustomButtonTooltipDirective } from './custom-button-tooltip.directive';

describe('CustomButtonTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomButtonTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
